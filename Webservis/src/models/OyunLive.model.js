 const mongoose = require("mongoose");

const OyunLive = mongoose.model(
  "OyunLive",
  new mongoose.Schema({
    //username: String,
   // email: String,
   // password: String,
    
  })
);

module.exports = OyunLive;

