 const mongoose = require("mongoose");

const Hakem = mongoose.model(
  "Hakem",
  new mongoose.Schema({
    //username: String,
   // email: String,
   // password: String,
    
  })
);

module.exports = Hakem;

