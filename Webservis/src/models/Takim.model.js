 const mongoose = require("mongoose");

const Takim = mongoose.model(
  "Takim",
  new mongoose.Schema({
    //username: String,
   // email: String,
   // password: String,
    
  })
);

module.exports = Takim;

