 const mongoose = require("mongoose");

const Sponsor = mongoose.model(
  "Ligler",
  new mongoose.Schema({
    //username: String,
   // email: String,
   // password: String,
    
  })
);

module.exports = Sponsor;

