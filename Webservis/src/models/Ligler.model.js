 const mongoose = require("mongoose");

const Ligler = mongoose.model(
  "Ligler",
  new mongoose.Schema({
    //username: String,
   // email: String,
   // password: String,
    
  })
);

module.exports = Ligler;

