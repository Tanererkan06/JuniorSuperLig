 const mongoose = require("mongoose");

const Veli = mongoose.model(
  "Veli",
  new mongoose.Schema({
    //username: String,
   // email: String,
   // password: String,
    
  })
);

module.exports = Veli;

