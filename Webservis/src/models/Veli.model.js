 const mongoose = require("mongoose");

const Sponsor = mongoose.model(
  "Veli",
  new mongoose.Schema({
    //username: String,
   // email: String,
   // password: String,
    
  })
);

module.exports = Sponsor;

